const path = require("path");

module.exports = {
  config: path.resolve("config", "config.js"),
  "migration-path": path.resolve("dist", "database", "migrations"),
  "seeder-path": path.resolve("dist", "database", "seeders"),
};
