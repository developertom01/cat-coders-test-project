export enum HttpErrorStatusCodes {
  BAD_REQUEST = 400,
  UNAUTHORIZE = 401,
  PAYMENT_REQUIRED = 402,
  FORBIDDEN = 403,
  NOTFOUND = 404,
  SERVER_ERROR = 500,
}

export enum HttpOkStatusCodes {
  OK = 200,
  CREATED = 200,
}
